# RAW Renamer Studio — Python sidecar
# Установка:  pip install -r requirements.txt
# (в build-macos.sh / build-sidecar.sh ставится в отдельный venv .build-venv)

# HTTP API
fastapi
uvicorn[standard]
python-multipart        # UploadFile/Form в FastAPI

# Заявки (xlsx/csv)
openpyxl

# НЕТ-проверка IMEI / внешние запросы
httpx

# Обработка превью/тинейбов RAW
Pillow
numpy

# Распознавание баркодов (опциональные движки — app работает и без них,
# но cv2+zxing нужны для полноценного сканера):
opencv-python-headless
zxing-cpp
# pyzbar — третий движок, требует системную libzbar (brew install zbar).
# Установите вручную, если нужен:  pip install pyzbar
