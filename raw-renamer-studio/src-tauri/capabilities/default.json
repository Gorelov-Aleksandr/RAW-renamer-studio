{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Базовые разрешения macOS-приложения: нативный диалог выбора папки (NSOpenPanel). Никаких лишних прав (камера/микрофон/файлы) не запрашивается.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
